### react component 라이프 사이클

- mount
- update
- unmount

**mount**
- 컴포넌트가 탄생하는 순간
- 화면에 처음 렌더링 되는 순간

**update**
- 컴포넌트가 다시 렌더링 되는 순간

**unmount**
- 컴포넌트가 화면에서 사라지는 순간

### useEffect
react component 의 사이드 이펙트(부수적인 효과, 파생되는 효과)를 제어하는 새로운 react hook
component 동작에 의한 파생되는 효과들
