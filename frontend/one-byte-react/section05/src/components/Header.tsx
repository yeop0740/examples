/**
 * 함수 컴포넌트
 * 컴포넌트는 아래와 같은 함수 컴포넌트, 화살표 함수 컴포넌트, 클래스 컴포턴트 형태로 작성할 수 있다.
 * 컴포넌트는 첫 글자를 대문자로 작성해야 한다.
 */
function Header() {
    return (
        <header>
            <h1>header</h1>
        </header>
    );
}

export default Header
