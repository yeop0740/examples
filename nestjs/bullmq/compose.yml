services:
  redis:
    image: redis:7-alpine
    ports:
      - '6382:6379'
    networks:
      - local
    healthcheck:
      test: ['CMD-SHELL', 'redis-cli ping | grep PONG']
      interval: 1s
      timeout: 3s
      retries: 3

  producer:
    build:
      context: ./producer
      dockerfile: Dockerfile
    ports:
      - '4000:3000'
    networks:
      - local
    depends_on:
      - redis

  consumer:
    build:
      context: ./consumer
      dockerfile: Dockerfile
    networks:
      - local
    depends_on:
      - redis
      - producer

networks:
  local:
    driver: bridge
